[tox]
requires =
    tox>4
    virtualenv>20.2
labels =
    testenv = py311
env_list =
    dependency
    lint
    create
    converge
    test-exec
    verify
    idempotence
    destroy
[testenv:dependency]
deps =
    -r requirements.txt
commands = molecule dependency

[testenv:lint]
deps =
    -r requirements.txt
commands =
    yamllint .
    flake8
    ansible-lint
[testenv:create]
setenv =
    DEPENDENCY_ENABLED=False
deps =
    -r requirements.txt
commands = molecule create {posargs}
[testenv:converge]
deps =
    -r requirements.txt
commands = molecule converge {posargs}
[testenv:test-exec]
deps =
    -r requirements.txt
commands = molecule test {posargs}
[testenv:verify]
deps =
    -r requirements.txt
commands = molecule verify {posargs}
[testenv:idempotence]
deps =
    -r requirements.txt
commands = molecule idempotence {posargs}
[testenv:destroy]
setenv =
    DEPENDENCY_ENABLED=False
deps =
    -r requirements.txt
commands =
    molecule destroy {posargs}
